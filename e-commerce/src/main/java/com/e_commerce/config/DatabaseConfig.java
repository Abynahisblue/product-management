package com.e_commerce.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class DatabaseConfig {
    // Database configuration is now handled through application.properties
    // AWS Parameter Store integration will be handled at runtime through environment variables
}