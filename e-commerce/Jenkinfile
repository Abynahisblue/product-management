pipeline {
    agent any


    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/Abynahisblue/product-management/tree/jenkins/e-commerce'
            }
        }



        stage('Build and Deploy with Docker Compose') {
            steps {
                script {

                    sh 'docker compose down'
                    sh 'docker compose build'
                }
            }
        }
    }

    post {
        success {
            echo 'Pipeline completed successfully!'
        }

        failure {
            echo 'Pipeline failed.'
        }
    }
}